name: CI_TECSUnit

on: [push]

jobs:
  TECSUnit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: TECSUnit
        run: |
          echo Hello
          cd hr-tecs/tecsgen/TECSUnit
          make clean
          make tecs
          make
          ./TECSUnit.exe
