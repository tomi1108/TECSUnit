name: learn-github-actions

on: [push]

jobs:
  CI_test_TECSUnit:
    runs-on: ubuntu-latest
    steps:
      - name: TECSUnit
      - uses: actions/checkout@v2
      - run: | 
          cd hr-tecs/tecsgen
          ls
          source set_env.sh
          cd TECSUnit
          make clean
          make tecs
          make
          ./TECSUnit.exe
